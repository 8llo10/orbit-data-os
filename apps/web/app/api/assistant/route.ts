import {NextResponse} from 'next/server';import {activeMembership} from '@/lib/auth';import {AppError,errorResponse} from '@/server/core/errors';import {can} from '@/server/core/permissions';import {loadWorkspaceSnapshot} from '@/server/modules/assistant/repository';import {runAssistant} from '@/server/modules/assistant/service';
export async function POST(req:Request){try{const membership=await activeMembership();if(!membership)throw new AppError('NO_WORKSPACE',404,'ما لقينا مساحة عمل مرتبطة بالحساب.');if(!can(membership.role,'assistant:use'))throw new AppError('FORBIDDEN',403,'ما عندك صلاحية لاستخدام ORBIT Assistant.');const body=await req.json().catch(()=>null) as {message?:unknown}|null;const message=typeof body?.message==='string'?body.message.trim():'';if(!message)throw new AppError('INVALID_MESSAGE',400,'اكتب سؤالك أولًا.');if(message.length>4000)throw new AppError('MESSAGE_TOO_LONG',400,'الرسالة طويلة جدًا. الحد 4000 حرف.');const snapshot=await loadWorkspaceSnapshot(membership.workspaceId,membership.role);const result=await runAssistant(message,snapshot);return NextResponse.json(result);}catch(error){return errorResponse(error)}}
