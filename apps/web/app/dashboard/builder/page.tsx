import AppShell from '@/components/AppShell';import DashboardBuilder from '@/components/DashboardBuilder';import {activeWorkspace} from '@/lib/auth';import {db} from '@orbit/db';import {redirect} from 'next/navigation';
export default async function Builder(){let ws;try{ws=await activeWorkspace()}catch{redirect('/login')}if(!ws)redirect('/login');let dashboard=await db.dashboard.findFirst({where:{workspaceId:ws.id},orderBy:{createdAt:'asc'}});if(!dashboard)dashboard=await db.dashboard.create({data:{workspaceId:ws.id,name:'Main dashboard',layout:['stats','collections','activity','system']}});const [collectionsCount,records,imports,automations,cols,activity]=await Promise.all([db.collection.count({where:{workspaceId:ws.id}}),db.dataRecord.count({where:{collection:{workspaceId:ws.id}}}),db.importJob.count({where:{workspaceId:ws.id}}),db.automationRule.count({where:{workspaceId:ws.id,status:'ACTIVE'}}),db.collection.findMany({where:{workspaceId:ws.id},include:{_count:{select:{records:true}}},take:6,orderBy:{records:{_count:'desc'}}}),db.auditLog.findMany({where:{workspaceId:ws.id},take:6,orderBy:{createdAt:'desc'}})]);const layout=Array.isArray(dashboard.layout)?dashboard.layout.filter((x):x is string=>typeof x==='string'):[];return <AppShell><div className="eyebrow">DASHBOARD BUILDER</div><h1 className="h1">Compose your command center.</h1><p className="lead">Reorder, remove and restore live widgets. No external dashboard service required.</p><DashboardBuilder dashboardId={dashboard.id} initial={layout} stats={{collections:collectionsCount,records,imports,automations}} collections={cols.map(c=>({name:c.name,records:c._count.records}))} activity={activity.map(a=>({action:a.action,at:a.createdAt.toISOString()}))}/></AppShell>}
