import AppShell from '@/components/AppShell';import {activeWorkspace} from '@/lib/auth';import {db} from '@orbit/db';import {redirect} from 'next/navigation';
export default async function Developer({searchParams}:{searchParams:Promise<{key?:string}>}){let ws;try{ws=await activeWorkspace()}catch{redirect('/login')}if(!ws)redirect('/login');const q=await searchParams;const [keys,hooks]=await Promise.all([db.apiKey.findMany({where:{workspaceId:ws.id},orderBy:{createdAt:'desc'}}),db.webhook.findMany({where:{workspaceId:ws.id},orderBy:{createdAt:'desc'}})]);return <AppShell><div className="eyebrow">DEVELOPER PLATFORM</div><h1 className="h1">Connect anything.</h1><p className="lead">Use ORBIT as a headless data layer through API keys, REST endpoints and outbound webhooks.</p>{q.key&&<div className="secretBox"><b>Copy this key now — it will never be shown again.</b><code>{q.key}</code></div>}<div className="developerGrid"><section className="card"><div className="eyebrow">API KEYS</div><h2>Programmatic access</h2><form className="inlineForm" method="post" action="/api/developer/keys"><input className="input" name="name" placeholder="Portfolio integration" required/><button className="btn">Generate</button></form><div className="keyList">{keys.map(k=><div className="settingRow" key={k.id}><div><b>{k.name}</b><p className="muted"><code>{k.prefix}••••••••</code> · {k.lastUsedAt?`used ${k.lastUsedAt.toLocaleDateString()}`:'never used'}</p></div><form method="post" action={`/api/developer/keys/${k.id}`}><button className="dangerLink">Revoke</button></form></div>)}</div></section><section className="card"><div className="eyebrow">WEBHOOKS</div><h2>Push events outward</h2><form className="form" method="post" action="/api/developer/webhooks"><input className="input" name="name" placeholder="My webhook" required/><input className="input" type="url" name="url" placeholder="https://example.com/hook" required/><button className="btn">Add endpoint</button></form>{hooks.map(h=><div className="settingRow" key={h.id}><div><b>{h.name}</b><p className="muted">{h.url}</p></div><form method="post" action={`/api/developer/webhooks/${h.id}/toggle`}><button className={`statusToggle ${h.active?'on':''}`}>{h.active?'ACTIVE':'PAUSED'}</button></form></div>)}</section></div><section className="card section"><div className="eyebrow">REST API</div><h2>Built-in endpoints</h2><div className="endpoint"><span className="method">GET</span><code>/api/v1/collections</code><p>List collections</p></div><div className="endpoint"><span className="method">GET</span><code>/api/v1/collections/:slug/records</code><p>Read records</p></div><div className="endpoint"><span className="method">POST</span><code>/api/v1/collections/:slug/records</code><p>Create a record and trigger automations</p></div><p className="muted">Send your key with the <code>x-api-key</code> header.</p></section></AppShell>}
