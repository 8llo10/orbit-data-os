import AppShell from '@/components/AppShell';import {activeWorkspace,requireUser} from '@/lib/auth';import {db} from '@orbit/db';import {redirect} from 'next/navigation';
export default async function Settings({searchParams}:{searchParams:Promise<{team?:string}>}){let user,ws;try{user=await requireUser();ws=await activeWorkspace()}catch{redirect('/login')}if(!ws)redirect('/login');const q=await searchParams;const members=await db.workspaceMember.findMany({where:{workspaceId:ws.id},include:{user:true},orderBy:{role:'asc'}});return <AppShell><div className="eyebrow">SYSTEM SETTINGS</div><h1 className="h1">Your orbit.</h1>{q.team==='not-found'&&<div className="errorBanner">That email does not have an ORBIT account yet. Ask them to register first.</div>}<div className="settingsGrid"><section className="card"><h2>Profile</h2><form className="form" method="post" action="/api/settings"><input type="hidden" name="type" value="profile"/><label>Name<input className="input" name="name" defaultValue={user.name||''}/></label><label>Email<input className="input" value={user.email} disabled/></label><button className="btn">Save profile</button></form></section><section className="card"><h2>Workspace</h2><form className="form" method="post" action="/api/settings"><input type="hidden" name="type" value="workspace"/><label>Workspace name<input className="input" name="name" defaultValue={ws.name}/></label><label>Slug<input className="input" value={ws.slug} disabled/></label><button className="btn">Save workspace</button></form></section><section className="card fullSpan"><div className="eyebrow">ACCESS CONTROL</div><h2>Workspace members</h2><form className="inlineForm" method="post" action="/api/settings/members"><input className="input" type="email" name="email" placeholder="Existing ORBIT user email" required/><select className="input compact" name="role"><option value="MEMBER">Member</option><option value="VIEWER">Viewer</option><option value="ADMIN">Admin</option></select><button className="btn">Add member</button></form><div className="memberTable">{members.map(m=><div className="settingRow" key={m.id}><div className="memberIdentity"><div className="avatar">{(m.user.name||m.user.email).slice(0,1).toUpperCase()}</div><div><b>{m.user.name||'Unnamed user'}</b><p className="muted">{m.user.email}</p></div></div>{m.role==='OWNER'?<span className="badge">OWNER</span>:<form className="inlineForm small" method="post" action={`/api/settings/members/${m.id}/role`}><select className="input compact" name="role" defaultValue={m.role}><option value="ADMIN">Admin</option><option value="MEMBER">Member</option><option value="VIEWER">Viewer</option></select><button className="btn secondary">Update</button></form>}</div>)}</div></section><section className="card"><h2>Architecture</h2><div className="specList"><span>PostgreSQL + Prisma</span><span>RBAC workspaces</span><span>Dynamic JSONB records</span><span>Schema inference</span><span>REST API + webhooks</span><span>Audit logging</span></div></section><section className="card"><h2>Privacy by design</h2><p className="muted">ORBIT's core data engine works without paid AI APIs. Your datasets stay in your configured PostgreSQL database.</p></section></div></AppShell>}
