import Link from 'next/link';import {currentUser} from '@/lib/auth';import {ArrowRight,Database,Network,Search,ShieldCheck,Workflow} from 'lucide-react';
export default async function Home(){const user=await currentUser();return <main className="landing"><nav className="landingNav"><div className="brandWrap"><div className="orb mini">◌</div><div className="brand">ORBIT<span className="brandDot">.</span></div></div><div className="landingLinks"><a href="#engine">Engine</a><a href="#platform">Platform</a><Link className="btn secondary" href="/login">Sign in</Link></div></nav><section className="hero"><div className="heroGlow"/><div className="heroCopy"><div className="eyebrow">OPEN-SOURCE PERSONAL DATA OPERATING SYSTEM</div><h1>Your scattered data,<br/><em>finally in orbit.</em></h1><p>Import spreadsheets and JSON. ORBIT infers structure, profiles quality, turns raw files into searchable collections, links datasets, exposes APIs and runs automations — without a paid AI dependency.</p><div className="heroActions"><Link className="btn big" href={user?'/dashboard':'/register'}>{user?'Open workspace':'Launch your workspace'} <ArrowRight size={17}/></Link><a className="btn secondary big" href="#engine">Explore the engine</a></div><div className="heroProof"><span>CSV · XLSX · JSON</span><span>PostgreSQL</span><span>REST API</span><span>Automations</span><span>Zero paid AI</span></div></div><div className="heroVisual"><div className="orbitRing r1"/><div className="orbitRing r2"/><div className="coreOrb"><b>ORBIT</b><span>DATA OS</span></div><div className="satellite s1">CSV</div><div className="satellite s2">API</div><div className="satellite s3">JSON</div><div className="satellite s4">SQL</div></div></section><section id="engine" className="landingSection"><div className="eyebrow">ONE SYSTEM · SIX CAPABILITIES</div><h2>Not another dashboard.<br/>A working data layer.</h2><div className="featureGrid">{[[Database,'Ingestion engine','Upload CSV, TSV, Excel or JSON. ORBIT normalizes rows and infers typed schemas automatically.'],[Search,'Universal search','Query records across every collection from one workspace-level search surface.'],[Network,'Data graph','Declare relationships between datasets and evolve isolated files into a connected model.'],[Workflow,'Automation engine','Trigger server-side rules from imports and record events, including outbound webhooks.'],[ShieldCheck,'Governed access','Session security, API keys, workspace boundaries and an immutable operational audit trail.'],[BracesIcon,'Developer platform','Use ORBIT headlessly with REST endpoints, pagination and programmatic access.']].map(([Icon,title,text])=>{const C=Icon as any;return <article className="featureCard" key={String(title)}><C size={22}/><h3>{String(title)}</h3><p>{String(text)}</p></article>})}</div></section><section id="platform" className="showcase"><div><div className="eyebrow">DATA PROFILING</div><h2>Understand a file the second it lands.</h2><p>ORBIT calculates missing values, uniqueness, distributions and numeric summaries from your real records. No token meter. No external inference bill.</p></div><div className="fakePanel"><div className="fakeTop"><span/><span/><span/></div><div className="fakeMetric"><small>DATA QUALITY</small><b>96%</b><i style={{width:'96%'}}/></div><div className="fakeRows"><span><b>revenue</b><i style={{width:'82%'}}/></span><span><b>region</b><i style={{width:'64%'}}/></span><span><b>status</b><i style={{width:'74%'}}/></span></div></div></section><footer className="landingFooter"><div className="brand">ORBIT<span className="brandDot">.</span></div><p>Open-source personal data infrastructure.</p></footer></main>}
function BracesIcon({size=22}:{size?:number}){return <span style={{fontSize:size,fontWeight:900}}>{'{ }'}</span>}
