export default function InsightBars({items}:{items:{value:string;count:number}[]}){const max=Math.max(1,...items.map(i=>i.count));return <div className="bars">{items.map(i=><div className="barRow" key={i.value}><div className="barLabel"><span>{i.value.slice(0,32)}</span><b>{i.count}</b></div><div className="barTrack"><i style={{width:`${Math.max(5,i.count/max*100)}%`}}/></div></div>)}</div>}
