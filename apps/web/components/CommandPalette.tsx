'use client';
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';
const items=[['Overview','/dashboard'],['Collections','/dashboard/collections'],['Import data','/dashboard/imports'],['Automations','/dashboard/automations'],['Global search','/dashboard/search'],['Data graph','/dashboard/graph'],['Dashboard builder','/dashboard/builder'],['API & Webhooks','/dashboard/developer'],['Activity log','/dashboard/activity'],['Settings','/dashboard/settings']];
export default function CommandPalette(){const [open,setOpen]=useState(false);const [q,setQ]=useState('');const router=useRouter();useEffect(()=>{const h=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(v=>!v)}if(e.key==='Escape')setOpen(false)};addEventListener('keydown',h);return()=>removeEventListener('keydown',h)},[]);if(!open)return <button className="searchTrigger" onClick={()=>setOpen(true)}>⌘K&nbsp;&nbsp;Quick command</button>;const filtered=items.filter(i=>i[0].toLowerCase().includes(q.toLowerCase()));return <div className="paletteBackdrop" onClick={()=>setOpen(false)}><div className="palette" onClick={e=>e.stopPropagation()}><input autoFocus className="paletteInput" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search commands…"/>{filtered.map(([label,url])=><button key={url} onClick={()=>{setOpen(false);router.push(url)}}>{label}<span>↗</span></button>)}</div></div>}
